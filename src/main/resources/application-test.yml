server:
  port: 8080
spring:
  datasource:
    url: ${NELO_TEST_DATABASE_URL_FOR_TESTS}:#{"NOT_PRESENT"}
    driverClassName: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
flyway:
  enabled: true
